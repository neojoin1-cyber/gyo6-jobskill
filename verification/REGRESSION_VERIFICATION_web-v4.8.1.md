# REGRESSION VERIFICATION - web-v4.8.1

| 항목 | 결과 |
|---|---|
| 기준 판정 | `FINAL_VERDICT_web-v4.8.0.md` (NO GO) |
| 검증일 | 2026-08-26 |
| 대상 | `sugar-salt-campus` / web 4.8.1 / Android 4.8.1 |
| 기술 판정 | **GO - 웹 배포 및 Android 내부 테스트** |

## 1. 차단 항목

| ID | 조치 | 실행 검증 | 판정 |
|---|---|---|---|
| V-B01 | 프런트의 demo 이메일·비밀번호·폴백 제거. Edge Function이 1회용 OTP 해시 발급. 기존 비밀번호와 세션 회전. 기기 60분·IP 시간당 12회 제한 | 소스·dist 전체 문자열 0건, 원격 OTP·메타데이터·쿨다운 검사 통과 | 해소 |
| V-B02 | 체험 종료·역할 전환 시 체험 중 생성된 `localStorage` 전체 삭제 | 실제 저장소 모의 시험에서 초안·근거·요약·부트스트랩 포함 0건 | 해소 |

## 2. 필수 수정 7건

| ID | 완료 내용 | 판정 |
|---|---|---|
| V-F01 | 작성 스크롤 영역 하단 여백을 sticky 작업바 높이와 안전영역만큼 확보. 마지막 입력칸과 작업바 비겹침 실측 | 해소 |
| V-F02 | 700px 이상 학생·교사 캠퍼스를 최대 1120px까지 확장. 교실 화면은 별도 전체폭 수업 뷰 유지 | 해소 |
| V-F03 | 클릭을 암시하던 문구를 `이 과목에서 제공하는 학습 기능`으로 교체 | 해소 |
| V-F04 | 서버 성취 기록이 있으면 `체험 계정의 예시 성취 기록`으로 표시하고 빈 상태 문구 제거 | 해소 |
| V-F05 | 학생·교사 헤더의 제품명을 `설탕과소금 스킬캠퍼스`로 통일 | 해소 |
| V-F06 | 금융·공공기관·대기업 분야별 기관·직무 placeholder 분리 | 해소 |
| V-F07 | IBK 4개, 한전 2개 표본을 603/656/676/609/658/681자로 보강하고 권장 범위 자동 검사 | 해소 |

## 3. 보안·원격 흐름

- `verify:public-trial`: 1회용 토큰, 역할 메타데이터, 읽기, 쓰기 `42501`, 동일 기기 쿨다운 통과.
- `verify:production-flows`: 학생·교사·학교관리자 로그인과 31개 원격 흐름 전부 통과.
- `verify:rls-isolation`: 인증 학생이 타 사용자 프로필, 타인 자기소개서 원본, 교사용 첨삭 RPC에 접근하지 못함.
- 신규 public 테이블에도 체험 쓰기 차단 트리거를 자동 부착하는 DB event trigger 적용.
- GitHub Pages 배포 작업에 역할 흐름, 체험 브로커, RLS 교차 접근 시험을 필수 단계로 추가.

## 4. 콘텐츠·UI 회귀

- 공식 정합성 1,334/1,334 통과.
- 전체 5,765문항 품질 위반 0건.
- 150개 학습 단원, 핵심 카드 921개, 실제 학습 문항 921개 렌더링 통과.
- 면접 기초 48단원·168문항, 심화 425문항, 기관 46곳, 자기소개서 7단계·30유형 확인.
- 자기소개서 12문항과 면접 240상황의 실전 선다형 품질 및 정답 위치 60/60/60/60 통과.
- 식음료서비스·품질경영 폐지 과목이 학생·교사 카탈로그, 오답 필터, 수업 덱, 미션에 노출되지 않음.
- 교사 체험 첨삭실에서 IBK·한전·면접 샘플 3건이 즉시 표시되고 체험 저장은 메모리에서만 동작.
- 첨삭실 모바일 최하단 도달: 최대 scrollTop 도달, 마지막 첨삭 버튼 화면 내 노출, 가로 넘침 0.

## 5. 브라우저 안정성

- 요청 뷰포트 320×720, 390×844, 768×1024, 1024×768, 1366×768, 1920×1080에서 교사 첨삭실 가로 넘침 0.
- 390px 학생 화면에서 캠퍼스 6개 학습관·전체 브랜드 노출, 가로 넘침 0.
- 웹 체험 10회 연속 재접속에서 빈 화면 0회, 모든 회차 앱 셸 표시.
- `gyo6.kr/learning-app.html`은 별도 자격증명을 갖지 않고 같은 GitHub Pages 앱을 iframe으로 사용함을 확인.

## 6. Android 4.8.1

| 항목 | 결과 |
|---|---|
| 버전 | `versionName 4.8.1`, `versionCode 29795102` |
| 빌드 | `clean lintRelease testReleaseUnitTest bundleRelease` 성공 |
| App Lint | 0건 |
| 서명 | release AAB 서명 성공 |
| AAB | `android/app/build/outputs/bundle/release/app-release.aab` (15,718,658 bytes) |
| SHA-256 | `695E39260FA9412297EDEFA6E71F87BD08AE9ADB116A016E9EF2CFA60429BF61` |
| 난독화 | R8 mapping 포함 |
| 네이티브 심볼 | `ndk.debugSymbolLevel FULL` 적용 |

## 7. 남은 사람 검증

- 실기기 또는 ADB가 이 작업 환경에 없어 이전 버전에서 4.8.1로 업그레이드하는 실기기 검증은 내부 테스트 설치 뒤 수행한다.
- 25분 수업 가능성은 교사·학생 파일럿으로만 판정할 수 있으므로 기술 출시 차단이 아니라 내부 테스트의 첫 확인 항목으로 이관한다.

## 최종 판정

웹 공개 체험 배포와 Google Play 내부 테스트 업로드를 진행할 수 있다. 광범위 공개 승격은 내부 테스트 기기에서 설치·업그레이드·진도 유지 확인 후 진행한다.
