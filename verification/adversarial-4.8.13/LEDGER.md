# JOB고 4.8.13 결함 원장

기준선: `ee6aee89abf8` (BASELINE.md · 커밋 2b0edcb)
웹 엔트리: `assets/index-CMR-vnJC.js` `19876d5f…da31` / AAB build 29803019 / Play 비공개 테스트

**원장 규칙**: 추가만 한다. 다른 담당의 행을 수정·삭제하지 않는다. 수정 전 반드시 다시 읽는다.
발견자가 증상을 쓰고, 원인 규명이 필요하면 `원인 가설`을 비운 채 남긴다.

## 요약

(코드 기준 2026-09-01 23:1X KST — 코워크 기록이 더해지면 각자 자기 몫을 더한다)

| 심각도 | 미해결 | 수정됨 | 재시험 통과 |
|---|---:|---:|---:|
| P0 | 0 | 0 | 0 |
| P1 | 3 | 0 | 0 |
| P2 | 1 | 0 | 0 |
| P3 | 1 | 0 | 0 |

---

## 결함 기록 양식 (복제해서 사용)

## [ID]
- **제목**: 사용자 결과 중심 한 문장
- **심각도**: P0 / P1 / P2 / P3
- **발견 담당**: 코드 / 코워크
- **기준선**: 커밋 [해시] / 웹 엔트리 [자산] / AAB build [번호] / Play [트랙]
- **환경**: 기기, OS, 브라우저/WebView, 화면 [px], 배율 [%], 네트워크 [조건]
- **사전조건**: 역할, 계정 상태, 데이터, 이전 행동
- **재현 절차**:
  1.
  2.
  3.
- **기대 결과**:
- **실제 결과**:
- **재현률**: 3회 중 [n]회
- **증거**: evidence/[ID].png, 콘솔·HAR·RPC 응답, questionId
- **영향**: 오학습 / 수업중단 / 데이터손실 / 권한침해 / 구매신뢰
- **원인 가설**: 확정 — / 추정 —
- **수정**: 커밋 [해시]
- **재시험**: 동일 절차 [n]회, 결과 [통과/실패]

---

## 문항 감사 행

| ID | 과목/단원 | 앱 정답 | 독립 정답 | 해설 일치 | 권리 | 판정 |
|---|---|---|---|---|---|---|
| | | | | | | |

---

## 기록

(여기부터 추가)

---

# 클로드 코드 기록 (기준선 ee6aee89abf8)

## TCH-001
- **제목**: 교사가 특정 단원을 지정해 미션을 내면 지정 문항이 한 개도 전달되지 않고 학생은 과목 전체 855문항에서 무작위로 받는다
- **심각도**: P1
- **발견 담당**: 코드
- **기준선**: 커밋 `2b0edcb` / 웹 엔트리 `assets/index-CMR-vnJC.js` / AAB build 29803019 / Play 비공개 테스트
- **환경**: 소스 경로 검증(런타임 모듈 그대로 실행). 화면 재현은 별도 진행 예정
- **사전조건**: 교사 계정, 과목 `job-common`, 미션 만들기에서 영역이 아닌 **단원 하나만** 선택
- **재현 절차**:
  1. `npx esbuild verification/adversarial-4.8.13/tools/probe-mission-delivery.mjs --bundle --platform=node --format=esm --loader:.json=json --outfile=.cache/probe-mission-delivery.mjs`
  2. `node .cache/probe-mission-delivery.mjs`
  3. "단원 1개만 선택했을 때" 절의 `[전체폴백]` 행을 본다
- **기대 결과**: 교사가 고른 단원의 문항만 학생에게 출제된다
- **실제 결과**: 다음 5개 단원은 지정 문항 0개가 전달되고 `job-common` 전체 855문항이 풀이 된다 —
  `의사소통 국어/복합 자료 종합·추론`(지정 5), `의사소통 영어/직무 영어 실전 보강`(9),
  `수리활용/자료 해석 종합·추론`(3), `문제해결/복합 제약 상황의 최적안 도출`(6),
  `직무적응/직무적응 검사 이해와 응답 전략`(8).
  `수리활용/표·그래프 수리활용`(5)은 area_ids 폴백으로 지정 밖 8문항이 나간다.
  교사·학생 어느 화면에도 경고가 없다
- **재현률**: 3회 중 3회 (결정적 — 출력 해시 동일)
- **증거**: `evidence/tch/mission-delivery.txt`, `evidence/tch/mission-delivery.run1~3.txt`
- **영향**: 수업중단 · 오학습 — 교사가 준비한 차시와 다른 내용이 학생에게 나가고, 교사는 그 사실을 모른다
- **원인 가설**:
  확정 — `buildJcMissionAreas()`가 노출하는 문항 id는 `jcStudyQuestions()` 산출물(변형 id 포함)이고,
  `MissionScreen`의 `QUESTION_POOLS['job-common']`은 `data/questions.json` + 영어 + 미디어 문항이라 id 집합이 다르다.
  교사가 고른 id 724개 중 361개가 미션 풀에 없다.
  확정 — id 매칭 실패 시 `MissionScreen.loadQuestions`가 접두 매칭 → area_ids 매칭 → **과목 전체**로 조용히 폴백한다
  (`src/screens/student/MissionScreen.jsx:290-315`).
  추정 — 폴백 사슬은 "빈 화면보다 낫다"는 의도로 보이나, 지정과 다른 출제를 오류 없이 내보내는 쪽이 더 위험하다
- **수정**: (미착수)
- **재시험**: (대기)

## TCH-002
- **제목**: 영역 단위로 미션을 내도 교사가 본 문항 수의 절반만 학생에게 전달된다
- **심각도**: P2
- **발견 담당**: 코드
- **기준선**: 커밋 `2b0edcb` / 웹 엔트리 `assets/index-CMR-vnJC.js` / AAB build 29803019 / Play 비공개 테스트
- **환경**: 소스 경로 검증
- **사전조건**: 교사 계정, 과목 `job-common`, 영역 전체 선택
- **재현 절차**: TCH-001과 같은 스크립트의 "영역 1개 선택했을 때" 절
- **기대 결과**: 교사 화면이 보여 준 문항 수만큼 출제 가능하다
- **실제 결과**: 지정 → 실제 풀이 `의사소통 국어` 85→38 · `의사소통 영어` 22→13 · `수리활용` 84→36 ·
  `문제해결` 382→191 · `직무적응` 145→85. 교사가 문항 수를 크게 잡으면 조용히 모자란 채로 출제된다
- **재현률**: 3회 중 3회
- **증거**: `evidence/tch/mission-delivery.txt`
- **영향**: 수업중단 — 문항 수 약속 불이행, 교사 계획과 실제 분량 불일치
- **원인 가설**: 확정 — TCH-001과 같은 id 집합 불일치(361/724). 영역 선택에서는 일부 id가 살아남아 전체폴백까지는 가지 않는다
- **수정**: (미착수)
- **재시험**: (대기)

## TCH-003
- **제목**: 연결형·단답형 문항이 미션에 섞이면 학생 화면에 보기가 하나도 없이 뜬다
- **심각도**: P1 (화면 재현 후 확정)
- **발견 담당**: 코드
- **기준선**: 커밋 `2b0edcb` / 웹 엔트리 `assets/index-CMR-vnJC.js` / AAB build 29803019 / Play 비공개 테스트
- **환경**: 소스 경로 검증. Playwright 화면 재현 예정
- **사전조건**: 교사가 `다양한 유형 보충 문항`·`자원관리`·`정보활용`·`기술활용`·`시스템적 사고`·`자기개발`·`직업윤리` 단원을 포함해 미션 생성
- **재현 절차**:
  1. `npx esbuild verification/adversarial-4.8.13/tools/probe-mission-question-types.mjs --bundle --platform=node --format=esm --loader:.json=json --outfile=.cache/probe-mqt.mjs`
  2. `node .cache/probe-mqt.mjs`
- **기대 결과**: 미션에 담기는 문항은 학생이 답할 수 있는 형태여야 한다
- **실제 결과**: 교사가 고를 수 있는 id 724개 중 60개가 `type: matching`(연결형)·`type: text`(단답형)이며,
  `MissionScreen`에는 이 두 유형의 렌더러가 없다. 8개 단원이 영향을 받는다
  (예: `문제해결/시스템적 사고` 14개, `문제해결/기술활용` 7개)
- **재현률**: 3회 중 3회 (정적 계산)
- **증거**: `evidence/tch/mission-question-types.run1~3.txt`
- **영향**: 수업중단 — 학생이 답을 고를 수 없고, 자율학습(StudyScreen)에서는 정상 동작하므로 학생은 앱 고장으로 인식한다
- **원인 가설**:
  확정 — `StudyScreen`은 `matching`·`text` 렌더러를 갖고 있으나(`src/screens/student/StudyScreen.jsx:70-73, 1314-1400`),
  `MissionScreen`은 `q.choices?.map(...)`만 그린다(`src/screens/student/MissionScreen.jsx:541-555`).
  추정 — 미션 경로가 선다형만 상정하고 만들어졌고, 뒤에 추가된 유형이 풀에서 걸러지지 않았다
- **수정**: (미착수)
- **재시험**: (대기)

## CNT-001
- **제목**: 정답은 맞는데 해설이 다른 선택지 번호를 정답이라고 말한다 (5문항)
- **심각도**: P1
- **발견 담당**: 코드
- **기준선**: 커밋 `2b0edcb` / 웹 엔트리 `assets/index-CMR-vnJC.js` / AAB build 29803019 / Play 비공개 테스트
- **환경**: 앱 런타임 문항 전수 2,481건 기계 감사
- **사전조건**: 채용필기 `대기업형` 트랙 학습
- **재현 절차**:
  1. `npx esbuild verification/adversarial-4.8.13/tools/audit-cnt-machine.mjs --bundle --platform=node --format=esm --loader:.json=json --outfile=.cache/audit-cnt-machine.mjs`
  2. `node .cache/audit-cnt-machine.mjs`
  3. `evidence/cnt/M1-explanation-number-mismatch.json` 확인
- **기대 결과**: 해설이 지목하는 번호와 채점 정답 번호가 같다
- **실제 결과**: 5문항에서 어긋난다 —
  `RW-enterprise-NCS-C206-basic-1`(정답 3번, 해설 "정답 1번"),
  `RW-enterprise-NCS-C206-basic-2`(정답 5번, 해설 "정답 3번"),
  `RW-enterprise-NCS-C206-standard-1`(정답 5번, 해설 "정답 2번"),
  `RW-enterprise-NCS-C206-standard-2`(정답 4번, 해설 "정답 2번"),
  `RW-enterprise-NCS-C209-diagnosis`(정답 1번, 해설 "정답 3번").
  보기 순서는 섞이지 않으므로(선택지 셔플 없음) 모든 학생이 같은 불일치를 본다
- **재현률**: 3회 중 3회 (전수 감사, 결정적)
- **증거**: `evidence/cnt/M1-explanation-number-mismatch.json`, `evidence/cnt/machine-audit.json`
- **영향**: 오학습 — 맞힌 학생이 자기 답을 틀렸다고 배우고, 틀린 학생은 엉뚱한 선택지를 정답으로 외운다
- **원인 가설**:
  확정 — 해설 본문이 고정 번호를 인용하는데 저장된 정답 인덱스와 다르다.
  추정 — 문항 생성 후 보기 순서가 바뀌었거나 해설이 다른 문항의 것을 복제했다.
  **어느 쪽이 옳은지(정답이 틀렸는지, 해설이 틀렸는지)는 코워크의 독립 채점 결과로 판정한다** — 5문항 모두 블라인드 세트에 추가함
- **수정**: (미착수)
- **재시험**: (대기)

## CNT-002 (관측 기록 — 결함 아님)
- **제목**: 정답 위치 분포에 통계적 편중이 없다
- **심각도**: INFO
- **발견 담당**: 코드
- **내용**: 4·5지선다 전수의 정답 번호 분포를 카이제곱으로 확인했다. 모든 층에서 임계값 미만 —
  `job-common` 4지 chi²=1.39 / 5지 5.95, `ncs-basic` 4지 4.58 / 5지 5.26,
  `recruit-written` 4지 9.27 / 5지 0.57, `interview` 4지 1.02 (임계 4지 16.27 · 5지 18.47, p=0.001).
  "정답 번호만 외워도 통과"는 이 축에서는 성립하지 않는다
- **증거**: `evidence/cnt/machine-audit.json` 의 `distribution`
- **비고**: 층화·중복·해설 정합과 별개 축이다. 본 항목은 통과 근거가 아니라 측정 기록이다

## CNT-003 (관측 기록)
- **제목**: 같은 문항이 채용필기 두 트랙에 서로 다른 id 로 중복 배치되어 있다 (71쌍)
- **심각도**: INFO (설계 의도 확인 필요)
- **발견 담당**: 코드
- **내용**: `RW-public-NCS-C113-*` 와 `RW-finance-NCS-C113-*` 처럼 발문·보기·정답이 같은 문항이
  공공기관형/금융권형에 각각 존재한다. 같은 트랙 안의 중복은 0건이다.
  두 트랙을 모두 학습하는 학생은 같은 문항을 다시 만난다
- **증거**: `evidence/cnt/machine-audit.json` 의 `M3-중복문항_층간(관찰)`
- **비고**: 트랙별 잔여역량 구성상 의도된 배치일 수 있어 결함으로 올리지 않는다. 소유자 확인 대상

## CNT-004
- **제목**: 지문(context)에 연속 공백이 남아 화면에서 줄 간격이 어긋난다 (4문항)
- **심각도**: P3
- **발견 담당**: 코드
- **대상**: `JC-SET-MA01-1`, `JC-SET-MA01-2`, `JC-SET-MA01-3`, `JC-SET-PS01-3` 의 `context`
- **재현 절차**: CNT-001과 같은 감사 스크립트, 코드 `M6-연속공백`
- **재현률**: 3회 중 3회
- **증거**: `evidence/cnt/machine-audit.json`
- **영향**: 국소 표현 — 오학습 위험 없음
- **원인 가설**: 확정 — 원문 정리 시 공백 3개 이상이 남았다

## 범위 제외 (소유자 결정 2026-09-01)
식음료서비스·품질경영 두 과목은 검증 범위에서 완전 제외한다.
두 과목의 앱 데이터도 비활성 상태이며(`src/lib/foodServiceBank.js`, `data/quality-mgmt-practice.json`),
의뢰서 5절 층화 기준의 "식음료서비스 단원별 10문항"은 적용하지 않는다. 결함으로 기록하지 않는다.
